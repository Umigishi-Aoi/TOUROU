import 'package:flutter/material.dart';

class SelfTourouTemplate extends StatelessWidget {
  const SelfTourouTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
