import 'package:flutter/material.dart';

class LanguageSettingTemplate extends StatelessWidget {
  const LanguageSettingTemplate({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}