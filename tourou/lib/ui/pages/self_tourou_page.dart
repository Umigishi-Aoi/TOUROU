// Flutter imports:
import 'package:flutter/material.dart';

class SelfTourouPage extends StatelessWidget {
  const SelfTourouPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
